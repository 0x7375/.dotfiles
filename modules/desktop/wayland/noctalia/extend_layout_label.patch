diff --git a/mangowc-layout-switcher/BarWidget.qml b/mangowc-layout-switcher/BarWidget.qml
index 036bf71..d4fee95 100644
--- a/mangowc-layout-switcher/BarWidget.qml
+++ b/mangowc-layout-switcher/BarWidget.qml
@@ -1,7 +1,7 @@
 import QtQuick
+import QtQuick.Layouts
 import Quickshell
 import qs.Commons
-import qs.Modules.Bar.Extras
 import qs.Services.UI
 import qs.Services.Compositor
 import qs.Widgets
@@ -19,49 +19,77 @@ Item {
   visible: CompositorService.isMango
 
   // ===== DATA BINDING =====
-  readonly property string layoutCode: (pluginApi?.mainInstance?.monitorLayouts ?? {})[screen?.name] || "?"
+  readonly property string screenName: screen?.name ?? ""
+  readonly property string layoutCode: (pluginApi?.mainInstance?.monitorLayouts ?? {})[screenName] || "?"
   readonly property string layoutName: pluginApi?.mainInstance?.getLayoutName(layoutCode) || layoutCode
+  readonly property int windowCount: (pluginApi?.mainInstance?.monitorWindowCounts ?? {})[screenName] || 0
 
   // ===== ICON MAPPING =====
   readonly property var iconMap: ({
-    "T":  "layout-sidebar",
-    "M":  "rectangle",
-    "S":  "carousel-horizontal",
-    "G":  "layout-grid",
-    "K":  "versions",
+    "T": "layout-sidebar",
+    "M": "rectangle",
+    "S": "carousel-horizontal",
+    "G": "layout-grid",
+    "K": "versions",
     "RT": "layout-sidebar-right",
     "CT": "layout-distribute-vertical",
     "TG": "layout-dashboard",
     "VT": "layout-rows",
     "VS": "carousel-vertical",
     "VG": "grid-dots",
-    "VK": "chart-funnel" 
+    "VK": "chart-funnel"
   })
 
-  // ===== SIZING =====
-  implicitWidth: pill.width
-  implicitHeight: pill.height
+  // ===== SIZING & CAPSULE =====
+  readonly property real capsuleHeight: Style.getCapsuleHeightForScreen(screenName)
+  readonly property real barFontSize: Style.getBarFontSizeForScreen(screenName)
 
-  // ===== COMPONENT =====
-  BarPill {
-    id: pill
+  readonly property real contentWidth: row.implicitWidth + Style.marginM * 2
+  readonly property real contentHeight: capsuleHeight
 
-    screen: root.screen
-    oppositeDirection: BarService.getPillDirection(root)
+  implicitWidth: contentWidth
+  implicitHeight: contentHeight
 
-    // Dynamic Icon
-    icon: root.iconMap[root.layoutCode] || "layout-board"
-    
-    // Text shown on hover
-    text: root.layoutName
-    tooltipText: "Layout: " + root.layoutName
+  Rectangle {
+    id: visualCapsule
+    x: Style.pixelAlignCenter(parent.width, width)
+    y: Style.pixelAlignCenter(parent.height, height)
+    width: root.contentWidth
+    height: root.contentHeight
+    color: mouseArea.containsMouse ? Color.mHover : Style.capsuleColor
+    radius: Style.radiusL
+    border.color: Style.capsuleBorderColor
+    border.width: Style.capsuleBorderWidth
 
-    onClicked: {
-      if (pluginApi) pluginApi.openPanel(root.screen, root)
+    RowLayout {
+      id: row
+      anchors.centerIn: parent
+      spacing: Style.marginS
+
+      NIcon {
+        icon: root.iconMap[root.layoutCode] || "layout-board"
+        pointSize: Style.fontSizeM
+      }
+
+      NText {
+        // Appends the window count next to the layout name
+        text: root.layoutCode === "M" ? `${root.layoutName} - ${root.windowCount}` : root.layoutName
+        pointSize: barFontSize
+        color: Color.mOnSurface
+      }
     }
+  }
+
+  MouseArea {
+    id: mouseArea
+    anchors.fill: parent
+    hoverEnabled: true
+    cursorShape: Qt.PointingHandCursor
+    acceptedButtons: Qt.LeftButton | Qt.RightButton
 
-    onRightClicked: {
-      if (pluginApi) pluginApi.openPanel(root.screen, root)
+    onClicked: mouse => {
+      if (pluginApi)
+        pluginApi.openPanel(root.screen, root);
     }
   }
 }
diff --git a/mangowc-layout-switcher/Main.qml b/mangowc-layout-switcher/Main.qml
index 7ed4164..3cf8116 100644
--- a/mangowc-layout-switcher/Main.qml
+++ b/mangowc-layout-switcher/Main.qml
@@ -30,6 +30,7 @@ Item {
 
   // ===== PUBLIC DATA =====
   property var monitorLayouts: ({})
+  property var monitorWindowCounts: ({})
   property var availableLayouts: []
   property var availableMonitors: []
 
@@ -75,6 +76,13 @@ Item {
         }
       }
     }
+
+    function updateWindowCount(monitor, count) {
+      if (monitor && root.monitorWindowCounts[monitor] !== count) {
+        root.monitorWindowCounts[monitor] = count;
+        root.monitorWindowCountsChanged();
+      }
+    }
   }
 
   // ===== PROCESSES =====
@@ -87,11 +95,15 @@ Item {
     
     stdout: SplitParser {
       onRead: line => {
-        // Only parse layout-related outputs to save regex cycles
-        if (line.includes(" layout ")) {
-          var match = line.match(/^(\S+)\s+layout\s+(\S+)$/)
-          if (match) {
-            internal.updateLayout(match[1], match[2])
+        var parts = line.trim().split(/\s+/);
+        if (parts.length < 3)
+          return;
+        if (parts[1] === "layout") {
+          internal.updateLayout(parts[0], parts[2]);
+        } else if (parts[1] === "tag" && parts.length >= 6) {
+          // column 6 is selection state, column 5 is window count
+          if (parts[5] === "1") {
+            internal.updateWindowCount(parts[0], parseInt(parts[4], 10));
           }
         }
       }
