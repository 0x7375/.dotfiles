diff --git a/src/cli/schema_dmenu.h b/src/cli/schema_dmenu.h
index 86d2d74b9..108c485ca 100644
--- a/src/cli/schema_dmenu.h
+++ b/src/cli/schema_dmenu.h
@@ -8,6 +8,7 @@ namespace noctalia::cli {
 
   inline constexpr std::array kDmenuFlags{
       Flag{"--prompt", "-p", "<text>", "Set the launcher prompt", {}, {}, false, false},
+      Flag{"--glyph", "-g", "<text>", "Set the icon used", {}, {}, false, false},
   };
   inline constexpr Command kDmenuCmd{
       "dmenu",
diff --git a/src/launcher/dmenu_cli.cpp b/src/launcher/dmenu_cli.cpp
index db246ce42..8551b98fa 100644
--- a/src/launcher/dmenu_cli.cpp
+++ b/src/launcher/dmenu_cli.cpp
@@ -44,6 +44,7 @@ namespace noctalia::launcher {
     if (parsed->helpRequested)
       return 0;
     const std::string prompt{parsed->value("--prompt")};
+    const std::string glyph{parsed->value("--glyph")};
 
     // Read all of stdin.
     std::string candidates;
diff --git a/src/launcher/dmenu_ipc.cpp b/src/launcher/dmenu_ipc.cpp
index c135fc918..bb0705807 100644
--- a/src/launcher/dmenu_ipc.cpp
+++ b/src/launcher/dmenu_ipc.cpp
@@ -214,16 +214,25 @@ void DmenuIpcService::finalizeSession(Session& session) {
   const std::string& buf = session.buffer;
   std::vector<std::string> lines;
   std::string prompt;
+  std::string glyph;
   std::size_t begin;
   const auto nl = buf.find('\n');
   if (nl == std::string::npos) {
     // No header newline: treat the whole payload as candidates.
     begin = 0;
   } else {
-    prompt = buf.substr(0, nl);
-    if (!prompt.empty() && prompt.back() == '\r') {
-      prompt.pop_back();
+    std::string header = buf.substr(0, nl);
+    if (!header.empty() && header.back() == '\r') {
+      header.pop_back();
     }
+
+    if (const auto null_pos = header.find('\0'); null_pos != std::string::npos) {
+      prompt = header.substr(0, null_pos);
+      glyph = header.substr(null_pos + 1);
+    } else {
+      prompt = header;
+    }
+
     begin = nl + 1;
   }
   for (std::size_t i = begin; i <= buf.size(); ++i) {
@@ -245,7 +254,7 @@ void DmenuIpcService::finalizeSession(Session& session) {
   const int connFd = session.connFd;
 
   auto provider = std::make_unique<DmenuStdinProvider>(
-      std::move(lines), providerId,
+      std::move(lines), providerId, glyph,
       [this, connFd](std::optional<std::string> selection) { completeSession(connFd, std::move(selection)); }
   );
   m_launcherPanel->addProvider(std::move(provider));
diff --git a/src/launcher/dmenu_stdin_provider.cpp b/src/launcher/dmenu_stdin_provider.cpp
index 2b2aaa2a0..77e2ec47d 100644
--- a/src/launcher/dmenu_stdin_provider.cpp
+++ b/src/launcher/dmenu_stdin_provider.cpp
@@ -15,8 +15,10 @@ namespace {
 
 } // namespace
 
-DmenuStdinProvider::DmenuStdinProvider(std::vector<std::string> lines, std::string id, Completion completion)
-    : m_id(std::move(id)), m_completion(std::move(completion)) {
+DmenuStdinProvider::DmenuStdinProvider(
+    std::vector<std::string> lines, std::string id, std::string glyph, Completion completion
+)
+    : m_id(std::move(id)), m_glyph(std::move(glyph)), m_completion(std::move(completion)) {
   m_lines.reserve(lines.size());
   for (auto& raw : lines) {
     if (raw.empty()) {
@@ -36,20 +38,22 @@ DmenuStdinProvider::DmenuStdinProvider(std::vector<std::string> lines, std::stri
 }
 
 std::vector<LauncherResult> DmenuStdinProvider::query(std::string_view text) const {
-  auto makeResult = [](const Line& line, double score) {
+  const std::string& activeGlyph = m_glyph.empty() ? "terminal" : m_glyph;
+
+  auto makeResult = [&activeGlyph](const Line& line, double score) {
     LauncherResult r;
     r.id = line.raw;
     r.title = line.title;
     r.subtitle = line.subtitle;
-    r.glyphName = "terminal";
+    r.glyphName = activeGlyph;
     r.score = score;
     return r;
   };
-  auto makeFreeformResult = [](const std::string& rawQuery) {
+  auto makeFreeformResult = [&activeGlyph](const std::string& rawQuery) {
     LauncherResult result;
     result.id = rawQuery;
     result.title = rawQuery;
-    result.glyphName = "terminal";
+    result.glyphName = activeGlyph;
     result.query = rawQuery;
     return result;
   };
diff --git a/src/launcher/dmenu_stdin_provider.h b/src/launcher/dmenu_stdin_provider.h
index 50ff0e45f..139da039d 100644
--- a/src/launcher/dmenu_stdin_provider.h
+++ b/src/launcher/dmenu_stdin_provider.h
@@ -16,7 +16,7 @@ public:
 
   // `lines` are raw candidate lines. `completion` fires exactly once: with the chosen
   // line on activate, or nullopt on cancel (panel closed without a selection).
-  DmenuStdinProvider(std::vector<std::string> lines, std::string id, Completion completion);
+  DmenuStdinProvider(std::vector<std::string> lines, std::string id, std::string glyph, Completion completion);
 
   [[nodiscard]] std::string_view defaultPrefix() const override { return ""; }
   [[nodiscard]] std::string_view id() const override { return m_id; }
@@ -31,12 +31,14 @@ private:
   struct Line {
     std::string raw;
     std::string title;
+    std::string glyph;
     std::string subtitle;
     std::string searchable;
   };
 
   std::vector<Line> m_lines;
   std::string m_id;
+  std::string m_glyph;
   Completion m_completion;
   bool m_completed = false;
 };
